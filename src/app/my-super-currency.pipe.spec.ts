import { MySuperCurrencyPipe } from './my-super-currency.pipe';

describe('MySuperCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new MySuperCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
